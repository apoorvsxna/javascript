let numbers = [1, 3, 7, 5, 0, 2];

numbers.sort();

numbers.reverse();

console.log(numbers);


let fruits = ["apple", "orange", "watermelon", "banana"];

fruits.sort(); // Also works with strings. Arranges in alphabetical order.

console.log(fruits);