Add a nested:true option such that a nested JSON can survive a round trip through CSV. When unparsing with it, each nested object or array should be flattened into column headers. Build the header list from the union of all rows in the order the columns are first seen.
When parsing back, read each header as a path and rebuild the original objects/arrays. Fill any skipped array positions with null. If a header isn't a valid path, leave it as a literal key and report an error.
If an object key itself contains one of the path characters, write it as a quoted segment in brackets instead (key=a.b becomes ["a.b"]).

 Your problem description says to add nested:true with flatten/unflatten rules, but the tests expect additional specifics not stated. Please clarify these so an implementer can reliably pass the tests: 1. Interface information [ERROR]: Missing value typing and encoding rules. Tests require parse to decode leaf cells to canonical JSON types (number, boolean, null) and require unparse to preserve string values that look like those types (e.g., '1', 'true', 'null', '007') without converting them, while keeping unambiguous scalars human-readable (no extra wrapping). This encoding/typing scheme is not described and is central to round-tripping types. 2. Interface information [ERROR]: Not specified that unparse must detect circular references and throw a controlled (non-RangeError) exception instead of recursing infinitely. 3. Interface information [WARNING]: Not specified that __parsed_extra must be preserved alongside the reconstructed nested object; tests assume this existing PapaParse behavior persists under nested mode. 4. Behaviors [WARNING]: While most behaviors align (flattening syntax, header union and columns honoring, quoted-key escaping, sparse arrays to null, invalid paths kept literal with an error), the additional expectations around canonical type decoding and minimal-wrapping for strings are extra and should be explicitly stated to avoid ambiguity.
