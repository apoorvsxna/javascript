// doesn't return anything, directly prints it
function wish(myName) 
{
    console.log(`Good morning ${myName}`);
};

const n = "apoorv"
wish(n);


// returns a value
function add(x, y)
{
    var ans = x + y;
    return ans;
}

console.log(add(57, 39));